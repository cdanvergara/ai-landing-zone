# Example terraform.tfvars file
# Copy this file to terraform.tfvars and update with your values

resource_group_name = "rg-ai-foundry-dev"
location            = "eastus"
ai_foundry_name     = "ai-foundry-dev"
ai_project_name     = "aiproject-dev"

# Subnet ID for private endpoints (from your existing VNet)
subnet_id = "/subscriptions/<subscription-id>/resourceGroups/<rg-name>/providers/Microsoft.Network/virtualNetworks/<vnet-name>/subnets/<subnet-name>"

# Optional: Private DNS Zone IDs (if already created in hub VNet)
# These zones should be created in your hub VNet and linked to your spoke VNet
private_dns_zone_ids = {
  cognitiveservices = "/subscriptions/<subscription-id>/resourceGroups/<hub-rg-name>/providers/Microsoft.Network/privateDnsZones/privatelink.cognitiveservices.azure.com"
  openai            = "/subscriptions/<subscription-id>/resourceGroups/<hub-rg-name>/providers/Microsoft.Network/privateDnsZones/privatelink.openai.azure.com"
}

# Optional: Deployment configuration (GPT-4o model)
deployment_model_name    = "gpt-4o-deployment"
deployment_model_version = "2024-11-20"
deployment_capacity      = 10

# Optional: Tags
tags = {
  Environment = "Development"
  Project     = "AI-Foundry"
  ManagedBy   = "Terraform"
}
